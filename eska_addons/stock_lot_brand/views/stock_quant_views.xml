<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record model="ir.ui.view" id="view_stock_quant_tree_editable_lot_brand">
        <field name="name">stock.quant.tree.editable.inherit</field>
        <field name="model">stock.quant</field>
        <field name="inherit_id" ref="stock.view_stock_quant_tree_editable"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='lot_id']" position="after">
                <field name="product_brand_id"/>
            </xpath>
        </field>
    </record>
    <record model="ir.ui.view" id="quant_search_view_lot_brand">
        <field name="name">stock.quant.tree.editable.inherit</field>
        <field name="model">stock.quant</field>
        <field name="inherit_id" ref="stock.quant_search_view"/>
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='productgroup']" position="after">
                <filter string="Brand"
                        name="brandgroup"
                        context="{'group_by': 'product_brand_id'}"
                />
            </xpath>
        </field>
    </record>
</odoo>
