<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="view_account_payment_form_disable_paired" model="ir.ui.view">
        <field name="name">account.payment.form.disable.paired</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='destination_journal_id']" position="after">
                <field name="available_destination_payment_method_line_ids" invisible="1"/>
                <field name="destination_payment_method_line_id"
                       options="{'no_create': True, 'no_open': True}"
                       attrs="{'invisible': [('is_internal_transfer', '=', False)], 'readonly': [('state', '!=', 'draft')], 'required': [('is_internal_transfer', '=', True),('state', '=', 'draft')]}"/>
            </xpath>
        </field>
    </record>
</odoo>